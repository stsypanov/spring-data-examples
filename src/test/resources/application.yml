spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  data:
    jpa:
      repositories:
        enabled: true
  main:
    banner-mode: "off"

spring.jpa.properties.hibernate.format_sql: true
